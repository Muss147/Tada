import {
  AlertTriangle,
  ArrowLeft,
  Loader2,
  Mail,
  Upload,
  User,
} from "lucide-react";

export const Icons = {
  spinner: Loader2,
  mail: Mail,
  arrowLeft: ArrowLeft,
  warning: AlertTriangle,
  upload: Upload,
  user: User,
} as const;
